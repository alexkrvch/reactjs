const Nav = () => {
    return (
        <nav className="app-nav">
            <a href="#">Profile</a>
            <a href="#">Message</a>
            <a href="#">Music</a>
            <a href="#">News</a>
            <a href="#">Settings</a>
        </nav>
    )
}

export default Nav;